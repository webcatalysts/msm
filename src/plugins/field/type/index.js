
class FieldHandler {
  constructor (options = {}) {
    this.options = options;
  }
  get type() {
    var ofType = typeof this.type;
    return ofType === 'undefined' ? ofType : this.type;
  }
  get isEmpty {
  }
  get fieldFormWidget {
  }
}

module.exports = exports = FieldHandler;

FieldHandler.prototype.toString = render

